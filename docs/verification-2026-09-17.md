# 2026-09-17 驗證紀錄

## 本機結果

- `npm test`：16 個測試檔、75 項測試全部通過。
- `npm run build`：成功；另檢查兩份題庫資產存在、內容與原始 JSON 相同，且建置後 JavaScript 指向正確檔名。
- `git diff --check`：無空白格式錯誤。

## 實際回歸案例

先重現計時導致整頁重繪與回頂，再修正為只更新倒數文字。整合測試載入真實 app，保留題卡節點並確認計時不捲動頁面。

14 項 app 互動測試涵蓋：末題交卷與單次結算、重新載入續答、英聽三選一與交卷前答案隱藏、逾時點選阻擋、離頁到期交卷、數學非選草稿與選擇題分開、科目年級題型篩選、猜對不計為答錯、寫作不評級與重做次數、官方單題複習不混入整卷非選、空白卷不給經驗值、完成下一份卷後仍可重開作文，以及壞掉的 session 不刪除玩家進度。

核心測試另外檢查同日重做、再次答錯、複習到期、非法答案與時間溢位。

## 獨立審查

審查包含會考來源分類、官方題數與答案卡、30 題原創的答案與題意、保存和交卷流程。審查指出的同日錯答處理、猜對計錯、時間溢位、空白交卷獎勵、草稿被下一次測驗覆蓋等問題已修正並加入測試；最終針對修正的再審查確認沒有剩餘問題。原創題未發現實質答案歧義；這不代表官方或教師審定。

## 實作範圍與未完成的驗證

- 官方題本文章、圖表與音檔仍由原站連結提供。本站提供計時答案卡；非選和寫作無自動評級。
- 原始答案 PDF 下載未成功，未完成逐頁影像目視比對；已核對可擷取表格文字、題數、欄位邊界與官方疑義決議。詳見 `official-sources.md`。
- 雲端瀏覽器無法連到本機預覽（`ERR_BLOCKED_BY_CLIENT`）；手機實機及視覺驗證未完成。自動化 DOM 互動驗證不等同手機實機測試。
- 完整報告與草稿保留最近 10 份，作答摘要保留最近 50 次。所有資料保存在目前瀏覽器，無跨裝置同步。

GitHub Pages 的測試及部署紀錄由專案 Actions 提供：
https://github.com/alger68/JHSEE-All-Subjects/actions
